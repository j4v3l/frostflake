# Plaintext template. Run `make secret-edit FILE=shared.yaml` to encrypt with sops.
pam_u2f_mappings: |
  # Each line should look like the output of `pamu2fcfg -u "$USER" >> ~/.config/Yubico/u2f_keys`
  # Example:
  # jager:cuHW6sPn1...long_key_blob...

# Add other shared secrets below. All values will be encrypted once you run sops.

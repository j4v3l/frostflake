# Frostflake VitePress Docs

This directory contains the VitePress documentation site for Frostflake. To start the docs locally:

```sh
cd docs/vitepress
npm install
npm run dev
```

The docs are styled to match the official NixOS documentation and are beginner-friendly.
